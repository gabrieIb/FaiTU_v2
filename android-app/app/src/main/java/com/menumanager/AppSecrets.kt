package com.menumanager

// Legacy configuration container for the former Google Sheets backend.
// Keeping the file avoids breaking imports for older modules, but Firestore release no longer uses it.
